import MarkdownEditor from '@/components/MarkdownEditor' // Fix the import path if necessary

export default function Home() {
  return (
    <main className="min-h-screen">
      <MarkdownEditor />
    </main>
  )
}
